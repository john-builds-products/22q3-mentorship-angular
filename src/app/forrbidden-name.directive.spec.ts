import { ForrbiddenNameDirective } from './forrbidden-name.directive';

describe('ForrbiddenNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForrbiddenNameDirective();
    expect(directive).toBeTruthy();
  });
});
